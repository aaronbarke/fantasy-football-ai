# Screenshots

Drop three PNGs here to make the top-of-README screenshot row render:

- `gameplan.png` — the /gameplan page (lineup + swap suggestions + win probability)
- `chat.png` — /chat mid-answer, so the streaming + grounded stats show
- `draft.png` — /draft with the ADP board, tier chips, and best-available panel visible

Aim for 1600x1000-ish, PNG, taken in light mode (readable on GitHub in both themes).
